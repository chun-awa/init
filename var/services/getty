#!/bin/sh
. /etc/init.d/rc.lib
. /etc/rc.conf

opentty(){
    setsid sh -c 'exec ${2} </dev/${1} >/dev/{1} 2>&1'
}

case "${1}" in
    start)
        log "Starting getty..."
        for i in ${TTY};do
            opentty "${i}" "getty 115200 ${i}"
        done
        ;;
esac
